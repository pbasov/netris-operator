
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: serverclustertemplates.k8s.netris.ai
spec:
  group: k8s.netris.ai
  names:
    kind: ServerClusterTemplate
    listKind: ServerClusterTemplateList
    plural: serverclustertemplates
    singular: serverclustertemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Number of VNets
      jsonPath: .spec.vnets
      name: VNets
      type: integer
    - jsonPath: .status.status
      name: Status
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ServerClusterTemplate is the Schema for the serverclustertemplates
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ServerClusterTemplateSpec defines the desired state of ServerClusterTemplate
            properties:
              vnets:
                description: VNets defines the list of VNet configurations for this
                  template
                items:
                  description: ServerClusterTemplateVNet defines a VNet configuration
                    within the template
                  properties:
                    ipv4DhcpEnabled:
                      description: IPv4DhcpEnabled enables DHCP for IPv4
                      type: boolean
                    ipv4Gateway:
                      description: IPv4Gateway configures the IPv4 gateway for this
                        VNet
                      properties:
                        allocation:
                          description: Allocation is the IP allocation pool (e.g.,
                            "10.188.0.0/16")
                          type: string
                        assignType:
                          description: AssignType specifies how the gateway is assigned
                            ("auto" or "manual")
                          enum:
                          - auto
                          - manual
                          type: string
                        childSubnetPrefixLength:
                          description: ChildSubnetPrefixLength is the prefix length
                            for child subnets
                          type: integer
                        hostnum:
                          description: Hostnum is the host number for the gateway
                          type: integer
                      type: object
                    ipv6DhcpEnabled:
                      description: IPv6DhcpEnabled enables DHCP for IPv6
                      type: boolean
                    ipv6Gateway:
                      description: IPv6Gateway configures the IPv6 gateway for this
                        VNet
                      properties:
                        allocation:
                          description: Allocation is the IP allocation pool (e.g.,
                            "10.188.0.0/16")
                          type: string
                        assignType:
                          description: AssignType specifies how the gateway is assigned
                            ("auto" or "manual")
                          enum:
                          - auto
                          - manual
                          type: string
                        childSubnetPrefixLength:
                          description: ChildSubnetPrefixLength is the prefix length
                            for child subnets
                          type: integer
                        hostnum:
                          description: Hostnum is the host number for the gateway
                          type: integer
                      type: object
                    postfix:
                      description: Postfix is appended to the cluster name to form
                        the VNet name
                      type: string
                    serverNics:
                      description: ServerNics lists the server NICs to connect to
                        this VNet
                      items:
                        type: string
                      type: array
                    type:
                      description: Type is the VNet type (e.g., "l2vpn", "l3vpn")
                      enum:
                      - l2vpn
                      - l3vpn
                      type: string
                    vlan:
                      description: Vlan specifies VLAN mode ("tagged" or "untagged")
                      enum:
                      - tagged
                      - untagged
                      type: string
                    vlanID:
                      description: VlanID is the VLAN ID ("auto" or a specific number)
                      type: string
                  required:
                  - postfix
                  - serverNics
                  - type
                  type: object
                type: array
            type: object
          status:
            description: ServerClusterTemplateStatus defines the observed state of
              ServerClusterTemplate
            properties:
              message:
                description: Message provides additional information about the status
                type: string
              status:
                description: Status represents the current provisioning status
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
